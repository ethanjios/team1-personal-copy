{% extends "layout.njk" %}

{% block content %}
<div class="container mx-auto max-w-md mt-8 p-6 bg-white rounded shadow">
    <h2 class="text-2xl mb-4">Apply for {{ roleName }}</h2>
    
    <!-- Error message container -->
    <div id="errorMessage" class="hidden bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
        <span id="errorText"></span>
    </div>
    
    <!-- Success message container -->
    <div id="successMessage" class="hidden bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
        <span id="successText">Application submitted successfully! Redirecting...</span>
    </div>
    
    <form id="applicationForm" method="POST" action="/application/{{ jobRoleId }}/apply" enctype="multipart/form-data" class="space-y-4">
        <div>
            <label for="cv" class="block mb-2 font-medium">CV Upload (Required)</label>
            <input type="file" id="cv" name="cv" accept=".pdf" required class="w-full p-2 border border-gray-300 rounded">
            <p class="text-gray-600 text-xs mt-1">PDF files only, max 5MB</p>
        </div>
        
        <div class="flex gap-4">
            <a href="/job-roles/{{ jobRoleId }}" class="btn-secondary">Cancel</a>
            <button type="submit" id="submitButton" class="btn-primary">Submit Application</button>
        </div>
    </form>
</div>
{% endblock %}